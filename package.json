{
  "name": "@aragon/apps",
  "version": "0.1.0",
  "description": "Aragon DAO Clients",
  "repository": "https://github.com/aragon/apps",
  "author": "Aragon Association",
  "license": "GNU",
  "private": true,
  "scripts": {
    "start": "yarn --cwd packages/web-app start",
    "build": "yarn --cwd packages/ui-components build && yarn --cwd packages/web-app build"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
